import React from 'react'
import bgImage from '../../../assets/images/pvt-reg/bg.png';
import rightArrow from '../../../assets/images/pvt-reg/rightArrow.svg';
import ZohoFormService from '../../child_components/ZohoformService';
import Image from 'next/image';
import { GoogleDiv } from '../GoogleDiv';

let hero = {
    "pack_includes": [
        "Process Consultation",
        "Application Drafting",
        "Guidance provider",
        "Expert Assistance",
        "Comprehensive documentation",
        "PAN India level services"

    ]
}
interface HeroProps {
  cityName: string;
}
export const Hero = ({ cityName }: HeroProps) => {
    return (
        <div className="hero align-center mobile-flex-column">
            <Image
                src={bgImage}
                alt="Background Image"
                layout="fill"
                objectFit="cover"
                objectPosition="center"
                priority={true}
                className="background-image"
            />
            <div className="header-content">
            <h4 className='mb-4'> WPC Certificate</h4>
                <p>An authority certificate known as a WPC certificate or WPC license enables Indian importers to purchase radio and wireless equipment from overseas. The Department of Telecommunication (D0T) is the entity that issues it. Maximize market potential with RegisterKaro WPC Certification.</p>
                <div className="packages">
                    {hero.pack_includes?.map((i: any, n: number) => {
                        return (
                            <div key={n} className='package flex w-80'>
                                <div>
                                    <Image src={rightArrow} alt='rightArrow' className='rightArrow' />
                                </div>
                                <div>{i}</div>
                            </div>
                        );
                    })}
                </div>
                <GoogleDiv />
            </div>
            <div className="zoho">
                <ZohoFormService service={'wpc-certificate'} />
            </div>
        </div>
    )
}
